server:
  port: 8081 # Set the server port to 8081
  servlet:
    context-path: /identity

spring:
  datasource:
    url: jdbc:mysql://27.77.115.46:3306/identity_service?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: identity_admin
    password: MyStrongPassword456@
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
  security:
    oauth2:
      resource server:
        jwt:
          issuer-uri: http://27.77.115.46:8080/realms/SmartHealth
  jpa:
    hibernate:
      ddl-auto: update # Options: create, create-drop, update, validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
keycloak:
  admin:
    server-url: http://27.77.115.46:8080/
    realm: SmartHealth
    client-id: admin-cli
    username: admin
    password: admin

