server:
  port: 8081 # Set the server port to 8081
  servlet:
    context-path: /identity

spring:
  datasource:
    url: jdbc:mysql://103.166.183.74:3306/identity_service?allowPublicKeyRetrieval=true&useSSL=false
    username: identity_admin
    password: MyStrongPassword456!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
  security:
      oauth2:
        resourceserver:
          jwt:
            issuer-uri: http://keycloak.smarthealth.io.vn/realms/SmartHealth
  jpa:
    hibernate:
      ddl-auto: update # Options: create, create-drop, update, validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
keycloak:
  admin:
    server-url: http://keycloak.smarthealth.io.vn/
    realm: SmartHealth
    client-id: smarthealth-app
    username: admin
    password: admin
  realm: SmartHealth
  auth-server-url: http://keycloak.smarthealth.io.vn/
  ssl-required: external
  public-client: true
  bearer-only: false
  client-id: smarthealth-app
  client-secret: EPqQ0BoKLRN3mgatc9VzWce43TcvNZU2
  username: admin
  password: admin123
logging:
  level:
    org.springframework.security: DEBUG
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    url: /v3/api-docs


